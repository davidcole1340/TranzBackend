version: '3'

services: 
  graphql:
    build: ./TranzQL
    restart: always
    ports:
      - '80:80'
    links:
      - mongo
    env_file:
      - ./.env
    environment: 
      - MONGO_HOST=mongo
  mongo:
    image: mongo
    ports:
      - '27017:27017'
      